# 面向 APT 攻击的大模型行为特征识别与研判研究 —— 系统设计方案



* 版本：v1.1

* 日期：2026-09-22

* 状态：完成（全部模块编码完成并通过接口 / 渲染 / 评测验证）

* 说明：本方案为**全新设计**，不复用 AutoSearch / AutoReport / AutoSC 的已有代码，仅沿用业界通用方法论（规则预筛 + LLM 研判分层、结构化输出标记、实体 - 关系图谱建模）。

* v1.1 变更记录：① 同步实际实现的 18 个 REST 接口与 7 个前端页面（新增数据管理 / 大模型分析 / 密钥配置三页）；② features.json 示例对齐实际 v0.3.0；③ 存储设计补充 enrichments / ioc_cache 两表；④ 补充 8.4 微步情报注入 LLM 溯源 Prompt 的实现流程；⑤ 真实数据来源补充实际采用的 OTRF 数据集；⑥ 实施计划补充 P8 阶段。



***

## 0. 设计原则与边界



1. **全新自研**：全部代码从零编写，不依赖既有三个工具的任何模块。

2. **前后端分离**：后端 Python FastAPI 提供 REST API，前端 Vue3 独立工程消费 API，通过 JSON 交互。

3. **知识图谱展示**：前端集成 AntV G6 渲染 "APT 组织 — 攻击手法 — 行为 —IOC— 事件" 实体关系图谱；后端构建图谱数据并提供接口。

4. **项目规模**：单机可跑通全流程，1–2 人约 10–14 天完成；数据用模拟生成（贴近真实 APT 手法），LLM 走 API（无 Key 时启用规则兜底模式）。

5. **完整覆盖 4 条核心要求**，但每项按合理体量实现：数据体系 / 识别研判 / 展示应急 / 动态更新与安全。



***

## 1. 项目概述

### 1.1 项目目标



1. 输入 APT 攻击事件数据（网络流量、进程日志、文件操作、DNS / 认证日志）。

2. 规则预筛降噪 → LLM 识别四类典型行为（长期潜伏、内网横向移动、隐蔽数据传输、痕迹清理）。

3. LLM 溯源研判：攻击组织、攻击路径、入侵入口、目标意图。

4. 输出结构化研判报告（行为 / 特征 / 影响范围 / 风险等级 / 处置建议），前端可视化展示 + 知识图谱 + 检索筛选 + 高风险告警提示。

5. 支持特征库 / 组织画像库动态更新；敏感字段加密存储；报告完整性校验。

### 1.2 范围界定



* 做：核心研判闭环、前后端分离应用、知识图谱、特征库热更新、加密与完整性校验、评测脚本、**情报平台接入（VT / 微步 / FOFA 增强与扩线）**、项目文档。

* 不做：实时流处理、多源设备采集对接、生产级高可用、在线训练学习。



***

## 2. 总体架构（前后端分离）



```
┌────────────────────────── 前端（Vue3 + Vite + TS）──────────────────────────┐

│  仪表盘页 │ 事件时间线 │ 研判报告页 │ 知识图谱页(G6) │ 检索筛选           │

│  ──────────────────────────────────────────────────────────────────────     │

│  API 封装层（axios + 拦截器 + 鉴权 Token）                                   │

└──────────────────────────────┬───────────────────────────────────────────────┘

&#x20;                              │ REST API（JSON）

┌──────────────────────────────▼───────────────────────────────────────────────┐

│                          后端（FastAPI）                                     │

│  路由层 api/：analyze │ events │ cases │ reports │ graph │ stats │ search │  │

│                     features │ verify │ intel │ data │ settings │ llm      │

│  ──────────────────────────────────────────────────────────────────────     │

│  服务层 services/：                                                          │

│  parser → prefilter → detector(LLM) → attribution(LLM) → filter → report    │

│  ──────────────────────────────────────────────────────────────────────     │

│  支撑层：llm\_client（API/兼容API/规则兜底）│ security（加密/校验）│ config    │

│  存储层：SQLite（events/cases/reports/enrichments/graph/ioc_cache）        │
│            │ data/（features.json、profiles、uploads、intel_reports）          │

└──────────────────────────────┬───────────────────────────────────────────────┘

&#x20;                              │

&#x20;  数据层：data\_gen 生成 events.json（模拟事件）+ labels.json（标准答案）

&#x20;  真实数据：datasets/atomic（OTRF Security-Datasets，Sysmon 事件）+ datasets/ioc（ThreatFox）

&#x20;  LLM：DeepSeek API（首选） / 通义·豆包等 OpenAI 兼容 API / 规则模板兜底
```

### 2.1 数据流



1. `data_gen.py` 生成带标签的模拟事件集 `events.json` 与标准答案 `labels.json`。

2. `parser.py` 将事件解析为统一 `Event` 对象（校验字段、时间排序）。

3. `prefilter.py` 按 `features.json` 规则打分，将海量事件聚合成少量可疑案例 `Case`。

4. `detector.py` 将 Case 交给 LLM，识别四类行为，输出行为标签 + 置信度 + **证据引用（事件 ID）**。

5. `attribution.py` 结合组织画像库（RAG-lite 检索），LLM 推理组织 / 路径 / 入口 / 意图。

6. `filter.py` 证据校验 + 置信度门限，过滤误报，产出最终研判结果。

7. `report.py` 生成结构化报告（含风险等级、处置建议）；`graph_builder.py` 构建知识图谱数据。

8. 结果入库 SQLite；前端经 REST API 拉取，渲染页面与知识图谱。

### 2.2 关键设计决策



1. **预筛在前、LLM 在后**：控制 LLM 调用量与上下文长度，兼顾实时性与成本。

2. **证据引用机制**：LLM 结论必须引用具体事件 ID，无引用或引用不足则降级，抑制幻觉。

3. **规则兜底**：无 LLM Key 时全流程仍可运行（模板推理），保证答辩可完整演示。

4. **图谱由分析结果驱动**：知识图谱不是手工绘制，而是由 "事件关联 + 研判结果 + 组织画像" 自动构建，体现系统能力。

5. **配置即更新**：特征规则、组织画像均为 JSON 文件，通过 API 热更新，满足 "动态更新" 要求。



***

## 3. 模块设计（分点罗列）

### 3.1 后端模块

#### B1 数据生成 `backend/data_gen.py`



1. 职责：生成带标签的模拟 APT 场景数据集。

2. 场景（3 个，手法参考公开 APT 报告）：

* 场景 A・朝鲜系：鱼叉钓鱼邮件 → 宏文档执行 → HTTP C2 回连 → 长期潜伏（低频 beacon）。

* 场景 B・南亚系：Web 弱口令 → SMB/WMI 内网横向移动 → 多台主机批量连接。

* 场景 C・伊朗系：DNS 隧道隐蔽外传数据 → 定时任务持久化 → 痕迹清理（日志删除、时间戳篡改）。

1. 输出：`events.json`（统一 schema）+ `labels.json`（标准答案，供评测）。

2. 关键点：正常流量噪声约占 70%；每场景 300–800 条事件；字段覆盖流量 / 进程 / 文件 / DNS / 认证五类。

#### B2 解析标准化 `backend/app/services/parser.py`



1. 职责：读取事件文件，清洗、校验、时间排序。

2. 输入：`events.json`（或用户上传的同类 JSON）。

3. 输出：`Event` 对象列表（dataclass/pydantic）。

4. 关键点：字段缺失默认值、非法时间戳过滤、事件按时间升序。

#### B3 特征规则库与预筛 `backend/app/services/prefilter.py` + `backend/data/features.json`



1. 职责：按规则对事件打分，聚合可疑事件为案例。

2. 规则覆盖四类行为（可配置，见 4.4）：

* 横向移动：445/SMB 批量连接、WMI 远程执行、同源多目标连接。

* 隐蔽传输：高熵子域名 DNS 请求、低频长连接、异常端口外连。

* 长期潜伏：凌晨登录、固定低频 beacon 间隔、长会话。

* 痕迹清理：wevtutil/sdelete 等清理进程、日志文件删除、时间戳篡改。

1. 输出：可疑案例 `Case`（按主机 + 时间窗聚合：事件窗口、可疑分、命中规则）。

2. 关键点：预筛把几千条事件压缩到个位数 Case；规则全部来自 `features.json`，支持热更新。

#### B4 LLM 客户端抽象 `backend/app/core/llm_client.py`



1. 职责：统一 LLM 调用接口，屏蔽后端差异。

2. 三种后端：DeepSeek API（首选）/ 任意 OpenAI 兼容 API / 规则模板兜底。

3. 接口：`generate(system, user) -> str`，内置超时、重试、错误降级。

4. 关键点：兜底模式用预筛规则 + 模板直接产出结构化结果，保证无 Key 可演示。

#### B5 行为识别 `backend/app/services/detector.py`



1. 职责：对可疑案例做 LLM 深度行为识别。

2. 输入：Case 事件窗口（截断到 token 预算内）。

3. 输出：`[Behavior{type, confidence, evidence:[event_id], description}]`。

4. 识别四类：长期潜伏、内网横向移动、隐蔽数据传输、痕迹清理。

5. 关键点：Prompt 强制输出 JSON + 证据事件 ID（见 8.1）；解析失败自动重试 1 次。

#### B6 溯源研判 `backend/app/services/attribution.py`



1. 职责：研判攻击组织、攻击路径、入侵入口、目标意图。

2. RAG-lite：从 `apt_profiles.json` 检索与当前行为特征最相似的组织画像（关键词 / 行为标签匹配，取 Top3）。

3. LLM 推理：给定 Case + 行为结果 + Top3 组织画像 → 输出归属结论与推理依据。

4. 输出：`Attribution{org, org_confidence, path:[step], entry, intent, reasoning}`。

5. 关键点：无匹配时输出 "疑似 / 未知组织"，不强行归因（避免编造）。

#### B7 误报过滤 `backend/app/services/filter.py`



1. 职责：多条件交叉校验，降低误报。

2. 规则：

* 证据校验：行为结论引用的有效事件数 ≥ 阈值（默认 2），不足则降级为 "低置信"。

* 规则交叉：LLM 行为结论与预筛命中规则一致性比对，明显冲突则标记复核。

* 置信度门限：confidence < 0.6 → "待人工复核"；≥ 0.8 → "确认"。

1. 输出：`FilterResult{final_behaviors, risk_level, status(确认/复核/误报)}`。

#### B8 报告生成 `backend/app/services/report.py`



1. 职责：生成结构化研判报告。

2. 报告内容：攻击行为与特征、影响范围（涉及主机 / IP / 域名）、风险等级（低 / 中 / 高 / 严重）、攻击链时间线、处置建议。

3. 处置建议（按行为类型给出三类）：阻断（封禁 IP / 域名、隔离主机、断外连）、清除（终止进程、删除文件 / 计划任务）、溯源（保全日志、提取 IOC、扩线查询）。

4. 输出：`Report` 对象 + Markdown/JSON 导出；落库 SQLite。

#### B9 知识图谱构建 `backend/app/services/graph_builder.py`



1. 职责：从事件、研判结果、组织画像构建图谱数据。

2. 节点类型：APT 组织 / 攻击行为 / 攻击手法 (TTP) / IP / 域名 / 样本 / 事件 / 目标领域。

3. 关系类型：使用 / 关联 / 涉及 / 具备 / 针对 / 利用（见第 6 章）。

4. 输出：`{nodes:[{id,label,type,props}], edges:[{source,target,relation,weight}]}`。

5. 关键点：图谱随分析结果动态生成，可导出 JSON/SVG。

#### B10 安全与配置 `backend/app/core/security.py` + `backend/app/core/config.py`



1. 安全：

* 敏感字段（IP / 域名 / 样本 Hash）落库前用 Fernet（AES）加密，展示时解密，密钥存本地 `keyfile`。

* 报告与图谱数据生成 HMAC-SHA256 签名，提供 `/api/verify` 校验接口。

* API 简单 Token 鉴权（前端登录页可选）。

1. 配置：统一读取 `config.yaml`（路径、模型、阈值、密钥位置）。

2. 动态更新：`features.json` / `apt_profiles.json` 支持 GET/PUT API 热更新，无需重启。

#### B11 FastAPI 路由层 `backend/app/api/`



1. 接口清单见第 5 章。

2. 统一返回格式：`{code, message, data}`；统一异常处理。

3. CORS 放行前端 dev 地址；`/docs` 提供 Swagger 文档。

#### B12 情报平台客户端 `backend/app/services/intel/clients.py`



1. 职责：统一封装情报平台查询接口（**全新实现**，按各平台官方 API 文档编写，不复用既有工具代码）。

2. 四个接入：

* `vt_client`：VT 免费 API——IP / 域名信誉、DNS↔IP 互查、检测结果。

* `threatbook_client`：微步威胁情报 API——IP / 域名恶意判定、威胁类型、家族信息。

* `fofa_client`：FOFA 高级 API—— 资产测绘查询（域名 / IP / 证书 / 指纹）。

* `qianxin_importer`：奇安信无公开 API—— 以公开报告 / 公开 IOC 导入组织画像库（不爬取网页）。

1. 关键点：API Key 从 `.env` 读取（gitignore）；全部异步 + 限速 + SQLite 缓存，控制免费额度。

#### B13 情报增强与扩线服务 `backend/app/services/enricher.py` + `expander.py`



1. 职责一（增强）：对报告内 IOC 批量查询 VT / 微步，生成 `Enrichment{ioc, platform, verdict, score, detail, source}`，写入报告 "情报支撑" 区块，并注入 LLM 溯源 Prompt 作为研判证据。

2. 职责二（扩线）：对确认恶意域名 / IP 调 FOFA 查询关联资产（同证书 / 同 IP / 同指纹），生成候选资产列表，标记 "疑似关联资产" 并写入知识图谱。

3. 输出：增强结果落库（敏感字段加密）；扩线结果返回前端供人工复核。

4. 关键点：查询失败降级为 "平台不可用"，不影响主流程；结果注明来源平台与查询时间。

### 3.2 前端模块（Vue3 + Vite + TypeScript）

#### F1 工程骨架



1. Vue3 + Vite + TS + Pinia（状态）+ Vue Router + Element Plus（UI）。

2. Axios 封装（baseURL、拦截器、错误提示、Token 注入）。

#### F2 概览仪表盘页 `Dashboard.vue`



1. 统计卡片：总事件数、可疑案例数、确认入侵数、高风险数。

2. ECharts：四类行为分布（柱状 / 雷达）、风险等级占比（饼图）、事件时间趋势（折线）。

#### F3 事件时间线与检索筛选 `Timeline.vue`



1. 事件 / 案例时间线视图（按场景或主机着色）。

2. 检索筛选：按情报类型（行为类型）、风险等级、时间范围、组织、关键词查询（调 `/api/search`）。

#### F4 研判报告详情页 `ReportDetail.vue`



1. 展示行为识别结果（类型、置信度、证据事件展开）。

2. 溯源结果（组织、路径、入口、意图）与推理依据。

3. 风险等级、影响范围、攻击链时间线、处置建议（阻断 / 清除 / 溯源分块）。

4. 完整性校验结果展示（调 `/api/verify`）。

#### F5 知识图谱页 `KnowledgeGraph.vue`（AntV G6）



1. 加载 `/api/graph` 数据，力导向布局渲染。

2. 节点按类型着色 + 图例；支持缩放 / 拖拽 / 点击节点查看详情抽屉。

3. 支持按节点类型过滤、按关系高亮、导出 PNG。

4. 点击 "APT 组织 / IP / 域名" 节点 → 联动跳转报告或检索。

#### F6 API 封装层 `src/api/`



1. 按资源拆分：analyze /events/cases /reports/graph /stats/search /features/verify /intel /data /settings /llm。

2. 全部走统一 Axios 实例。

#### F7 数据管理页 `DataLab.vue`

1. 数据源列表：内置模拟数据 / OTRF 真实数据集 / 用户上传文件，支持切换。

2. 上传 JSON / NDJSON / CSV / 文本（扩展名白名单 + 50MB 上限），入库前解析预览。

#### F8 大模型分析页 `LLMAnalysis.vue`

1. 选择数据源 + 输入分析问题，调用 LLM 深度研判（无 Key 时规则兜底）。

2. 回答以轻量 Markdown 渲染（标题 / 列表 / 代码块，HTML 转义防 XSS）。

#### F9 密钥配置页 `Settings.vue`

1. 配置 DeepSeek / VT / 微步 / FOFA API Key，Fernet 加密落盘 + 热更新生效。

2. 实时展示各平台可用状态与 LLM 模式（API / 兜底）。



***

## 4. 数据结构设计

### 4.1 事件 Event



| 字段                        | 类型       | 说明                                       |
| ------------------------- | -------- | ---------------------------------------- |
| event\_id                 | str      | 唯一 ID                                    |
| ts                        | str(ISO) | 时间戳                                      |
| type                      | enum     | flow / process / file / dns / auth       |
| src\_ip / src\_port       | str/int  | 源地址                                      |
| dst\_ip / dst\_port       | str/int  | 目的地址                                     |
| domain                    | str      | DNS 查询域名                                 |
| process / parent\_process | str      | 进程与父进程                                   |
| file\_path / file\_hash   | str      | 文件路径与哈希                                  |
| action                    | str      | 操作类型（login/exec/delete/write/connect...） |
| user / host               | str      | 账户与主机                                    |
| scene                     | str      | 所属场景（评测用）                                |
| label                     | enum     | normal /attack（标准答案，评测用）                 |

### 4.2 可疑案例 Case



| 字段                                      | 类型               | 说明          |
| --------------------------------------- | ---------------- | ----------- |
| case\_id                                | str              | 案例 ID       |
| host                                    | str              | 关联主机        |
| window\_start / window\_end             | str              | 时间窗口        |
| events                                  | list\[event\_id] | 窗口内事件（截断上限） |
| suspicious\_score                       | float            | 预筛得分        |
| hit\_rules                              | list\[str]       | 命中规则名       |
| behaviors / attribution / final\_status | -                | 后续流水线填充     |

### 4.3 研判结果 Report



| 字段                       | 说明                      |
| ------------------------ | ----------------------- |
| report\_id / created\_at | 元信息                     |
| behaviors                | 行为类型 + 置信度 + 证据事件       |
| attribution              | 组织 / 路径 / 入口 / 意图 + 置信度 |
| risk\_level              | 低 / 中 / 高 / 严重          |
| scope                    | 影响主机 / IP / 域名列表        |
| timeline                 | 攻击链时间线（阶段→事件）           |
| recommendations          | 阻断 / 清除 / 溯源三类建议        |
| status                   | 确认 / 待复核 / 误报           |
| signature                | HMAC 完整性签名              |

### 4.4 特征规则 features.json（示例）



```json
{
  "version": "0.3.0",
  "behaviors": {
    "lateral_movement": {
      "name": "内网横向移动", "threshold": 3,
      "event_rules": [
        {"field": "dst_port", "op": "eq", "value": 445, "weight": 2},
        {"field": "process", "op": "in", "value": ["wmic","psexec","smbexec","covenant","mimikatz"], "weight": 3}
      ],
      "agg_rules": [{"field": "distinct_dst_ip_count", "op": "gte", "value": 5, "weight": 3}]
    },
    "hidden_channel": {
      "name": "隐蔽数据传输", "threshold": 3,
      "event_rules": [
        {"field": "domain_entropy", "op": "gte", "value": 3.2, "weight": 3},
        {"field": "type", "op": "eq", "value": "dns", "weight": 1}
      ],
      "agg_rules": []
    },
    "long_term_latency": {
      "name": "长期潜伏", "threshold": 3,
      "event_rules": [{"field": "hour", "op": "between", "value": [0,5], "weight": 2}],
      "agg_rules": [{"field": "beacon_interval_stable", "op": "eq", "value": true, "weight": 3}]
    },
    "trace_cleaning": {
      "name": "痕迹清理", "threshold": 3,
      "event_rules": [
        {"field": "process", "op": "in", "value": ["wevtutil","sdelete","timestomp","eventvwr"], "weight": 3},
        {"field": "action", "op": "in", "value": ["delete","overwrite"], "weight": 2}
      ],
      "agg_rules": []
    }
  }
}
```

> 说明：实现版本为 v0.3.0。规则分 `event_rules`（事件级打分）与 `agg_rules`（窗口聚合级：`distinct_dst_ip_count` 采用 30 分钟滑动子窗口统计，防止长潜伏窗口累计误报；`beacon_interval_stable` 要求同目标低频规律回连）。

### 4.5 组织画像 apt\_profiles.json（示例）



```
{

&#x20; "apts": \[

&#x20;   {

&#x20;     "name": "Kimsuky",

&#x20;     "region": "朝鲜",

&#x20;     "targets": \["政府","外交","智库"],

&#x20;     "behaviors": \["隐蔽数据传输","长期潜伏"],

&#x20;     "ttps": \["鱼叉钓鱼","宏文档","HTTP C2","PowerShell"],

&#x20;     "tools": \["BabyShark","AppleSeed"],

&#x20;     "keywords": \["kimsuky","babyshark","appleseed"]

&#x20;   }

&#x20; ]

}
```

### 4.6 知识图谱数据模型



* Node：`{id, label, type, props{...}}`


  * type：`apt / behavior / ttp / ip / domain / sample / case / target`

* Edge：`{source, target, relation, weight}`


  * relation：`使用 / 关联 / 涉及 / 具备 / 针对 / 利用`



***

## 5. 后端 API 设计（REST）



| 方法      | 路径                       | 说明                         |
| ------- | ------------------------ | -------------------------- |
| POST    | /api/analyze             | 运行全流程（可指定场景），返回 report\_id |
| GET     | /api/events              | 事件分页（支持场景 / 类型 / 时间过滤）     |
| GET     | /api/cases               | 可疑案例列表                     |
| GET     | /api/reports/{id}        | 研判报告详情                     |
| GET     | /api/graph               | 知识图谱 {nodes, edges}        |
| GET     | /api/stats               | 仪表盘统计数据                    |
| POST    | /api/search              | 组合检索：类型 / 等级 / 时间 / 关键词    |
| GET     | /api/features            | 获取特征规则库                    |
| PUT     | /api/features            | 更新特征规则库（热更新）               |
| GET/PUT | /api/profiles            | 组织画像库读取 / 更新               |
| GET     | /api/verify/{report\_id} | 报告完整性校验结果                  |
| GET     | /api/intel               | 单点情报查询（IP / 域名，聚合多平台结果）    |
| POST    | /api/enrich              | 对指定报告 IOC 批量情报增强（VT / 微步）  |
| POST    | /api/expand              | 对指定 IOC 进行 FOFA 资产扩线       |
| POST    | /api/enrichments         | 查询增强 / 扩线结果（按报告或 IOC）      |
| POST    | /api/events/batch        | 按事件 ID 批量查询（证据列表按需拉取）    |
| GET     | /api/data/sources        | 数据源列表（模拟 / OTRF / 上传）        |
| POST    | /api/data/upload         | 上传数据文件（扩展名白名单 + 大小上限）    |
| GET     | /api/data/source/{id}    | 指定数据源详情（含事件预览）           |
| GET     | /api/settings            | 密钥 / LLM / 情报平台可用状态          |
| POST    | /api/settings/keys       | 保存密钥（Fernet 加密 + 热更新）       |
| POST    | /api/llm/analyze         | 大模型开放式分析（数据源 + 问题）        |



***

## 6. 知识图谱设计



1. **实体（节点）**：

* APT 组织（来自画像库 / 研判结果）

* 攻击行为（识别结果：潜伏 / 横移 / 隐蔽传输 / 清痕）

* 攻击手法 TTP（画像库 + LLM 提取）

* IP / 域名 / 样本（事件中出现，敏感字段）

* 事件（Case/Report 节点）

* 目标领域（组织画像 targets）

1. **关系（边）**：

* `APT组织 --使用--> 攻击手法`

* `APT组织 --关联--> IP/域名/样本`

* `事件 --涉及--> APT组织`

* `事件 --具备--> 攻击行为`

* `攻击行为 --利用--> 攻击手法`

* `APT组织 --针对--> 目标领域`

* `IP/域名 --归属--> APT组织`

1. **构建规则**：

* 事件中 IOC 与画像库关键词 / 标签匹配 → 组织关联边。

* 研判行为 → 行为节点；行为与画像 TTP 交集 → 手法节点。

* 每次分析后重建图谱，保证 "图谱 = 分析结果的可视化"。

1. **前端交互（G6）**：

* 力导向布局 + 节点类型着色 + 图例。

* 点击节点 → 详情抽屉（属性 + 关联关系）。

* 按类型过滤、关系高亮、导出 PNG。

* 点击组织 / IOC 节点 → 跳转检索或报告。



***

## 7. 技术选型



| 环节      | 选型                              | 理由                       |
| ------- | ------------------------------- | ------------------------ |
| 后端框架    | Python 3.14 + FastAPI + uvicorn | 异步、自动 Swagger 文档、演示友好      |
| 数据校验    | pydantic                        | 事件 / 报告模型校验              |
| 存储      | SQLite（标准库 sqlite3）             | 单机零部署                    |
| LLM 调用  | requests/httpx                  | DeepSeek / OpenAI 兼容 API |
| 加密 / 校验 | cryptography（Fernet + HMAC）     | 满足敏感数据加密与完整性要求           |
| 前端框架    | Vue3 + Vite + TypeScript        | 前后端分离主流，脚手架快             |
| UI 组件   | Element Plus                    | 表格 / 表单 / 抽屉现成           |
| 图表      | ECharts                         | 仪表盘 / 时间线 / 分布图          |
| 知识图谱    | AntV G6                         | 图可视化专业、中文文档完善            |
| 状态管理    | Pinia + Vue Router              | 页面与状态组织                  |



***

## 8. 提示词工程

### 8.1 行为识别 Prompt（detector）



1. 系统提示：你是资深安全分析师，根据事件序列识别 APT 攻击行为。

2. 用户输入：四类行为定义（潜伏 / 横移 / 隐蔽传输 / 清痕）+ Case 事件窗口（截断）。

3. 输出约束（JSON）：



```
{

&#x20; "behaviors": \[

&#x20;   {"type": "lateral\_movement", "confidence": 0.9,

&#x20;    "evidence": \["evt\_0012","evt\_0013"], "description": "..."}

&#x20; ]

}
```



1. 强制要求：confidence 0–1；evidence 必须来自给出的事件 ID；无行为输出空数组。

### 8.2 溯源研判 Prompt（attribution）



1. 系统提示：你是威胁情报专家，基于行为特征研判攻击组织。

2. 用户输入：Case 摘要 + 行为识别结果 + Top3 组织画像（RAG-lite 检索）。

3. 输出约束（JSON）：



```
{

&#x20; "org": "Kimsuky", "org\_confidence": 0.7,

&#x20; "path": \["钓鱼邮件","宏文档","C2回连","数据外传"],

&#x20; "entry": "鱼叉钓鱼", "intent": "窃取外交情报",

&#x20; "reasoning": "行为特征与画像库匹配点：..."

}
```



1. 强制要求：画像库无匹配时 org 填 "疑似未知组织" 并降低置信度，禁止编造。

### 8.3 输出解析与容错



1. 提取 JSON（容忍模型输出前后杂讯：取首个 `{` 到末尾 `}`）。

2. JSON 解析失败 → 重试 1 次 → 仍失败则用规则兜底结果。

3. 字段缺失用默认值，不中断流水线。

### 8.4 微步情报注入 LLM 溯源 Prompt 的实现流程

1. **触发时机**：`pipeline._process_case` 在行为识别之后、溯源之前调用 `enricher.enrich_report`，对案例 IOC（域名 / 外网 IP）并发查询微步 + VT（各平台独立限速 1 req/s，SQLite 缓存 TTL 24h）。

2. **情报压缩**：`enricher.to_prompt_lines` 仅保留判定为「恶意 / 可疑」的条目，压缩为每行一条的注入文本：`- {ioc} {平台}[{判定}] 评分{score} 家族/{family} 标签:{tags}`，最多 8 条。

3. **注入位置**：溯源用户提示词末尾追加「【情报支撑】」段落（见下），未命中恶意情报时输出占位句「（无外部情报支撑，请仅基于行为特征研判）」。

```
【情报支撑】（微步 / VirusTotal 判定，仅供参考）
- 45.77.10.20 threatbook[恶意] 评分75 家族/CobaltStrike 标签:C2,beacon
- c2-a.secure-update.net virustotal[恶意] 评分62 标签:malicious-domain
```

4. **LLM 使用约束**：提示词要求组织判定必须引用情报行或行为证据，两者皆无时输出「疑似未知组织」并降低置信度，禁止编造家族归属。

5. **链路**：enricher（并发查询 + 缓存）→ to_prompt_lines（压缩）→ attribution（Prompt 注入 + RAG-lite 画像匹配）→ 报告「情报支撑」区块 + 图谱 TTP/家族节点。

***

## 9. 存储设计（SQLite）



| 表       | 关键字段                                                                                                                                            | 说明                                    |
| ------- | ----------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------- |
| events  | event\_id, ts, type, src\_ip, dst\_ip, domain, process, file\_hash, scene, label                                                                | 加密列：src\_ip/dst\_ip/domain/file\_hash |
| cases   | case\_id, host, window, score, hit\_rules, status                                                                                               | 预筛与流水线状态                              |
| reports | report\_id, created\_at, risk\_level, status, behaviors\_json, attribution\_json, scope\_json, timeline\_json, recommendations\_json, signature | 完整报告                                  |
| graph   | graph\_id, created\_at, nodes\_json, edges\_json                                                                                                | 知识图谱快照（nodes 含 IOC 明文，加密落库）            |
| enrichments | enrichment\_id, report\_id, ioc, ioc\_type, platform, verdict, score, family, detail, source\_url, queried\_at            | IOC 多平台增强结果（ioc/source\_url 加密）          |
| ioc\_cache | cache\_key, payload\_json, queried\_at                                                                                                          | 情报平台查询缓存（TTL 24h，省免费额度）             |



***

## 10. 实施计划



| 阶段 | 内容            | 产出                                                              | 工作量                     |
| -- | ------------- | --------------------------------------------------------------- | ----------------------- |
| P1 | 后端骨架 + 数据生成   | FastAPI 工程、data\_gen、parser、Event 模型                            | 2 天                     |
| P2 | 特征库 + 预筛      | features.json、prefilter、Case 聚合                                 | 1.5 天                   |
| P3 | LLM 识别 + 溯源   | llm\_client、detector、attribution、提示词                            | 2.5 天                   |
| P4 | 过滤 + 报告 + 图谱  | filter、report、graph\_builder、API 路由                             | 1.5 天                   |
| P5 | 前端核心页 + G6    | Vue 工程、Dashboard/Timeline/Report/KnowledgeGraph                 | 3 天                     |
| P6 | 安全 + 评测 + 文档  | security、evaluate.py、README、报告素材                                   | 1.5 天                   |
| P7 | 情报平台接入 + 前端扩展 | intel 客户端（VT / 微步 / FOFA）、enricher、expander、报告 "情报支撑" 区块、图谱扩线节点 | 2–2.5 天                 |
| P8 | 数据管理 + LLM 分析 + 密钥配置 | DataLab（上传 / 数据源切换）、LLMAnalysis（开放式研判问答）、Settings（Fernet 加密密钥热更新） | 1.5 天                   |
| 合计 | -             | -                                                               | 单人约 16–17 天，双人约 11–13 天 |



***

## 11. 测试与验收

### 11.1 后端测试（pytest）



1. parser：字段校验、非法数据过滤、时间排序。

2. prefilter：规则命中、Case 聚合正确性。

3. prompt 解析：JSON 容错、重试逻辑。

4. security：加密解密往返、HMAC 校验通过 / 篡改失败。

5. API：analyze → reports → graph 全链路冒烟。

### 11.2 评测指标（evaluate.py）



1. 对 `labels.json` 标准答案，逐行为计算 Precision / Recall / F1。

2. 整体准确率（行为识别正确数 / 总标签数）。

3. 误报率：正常事件被误判为攻击的比例。

4. 输出评测报告（JSON/Markdown），用于答辩数据支撑。

### 11.3 需求逐条验收对照



| 需求                                    | 本方案落点                                                              |
| ----------------------------------- | ------------------------------------------------------------------ |
| 1. 特征与数据体系（行为 / 流量 / 恶意代码特征；多源数据采集） | features.json 四类行为规则 + 五类事件模拟数据（流量 / 进程 / 文件 / DNS / 认证）           |
| 2. 行为识别与研判（四类典型行为；溯源；误报过滤）          | detector（LLM 识别）+ attribution（LLM 溯源，RAG-lite）+ filter（证据 / 置信度过滤） |
| 3. 展示与应急（报告；实时告警；处置指引）              | 前端报告页 + 风险等级标红告警 + 阻断 / 清除 / 溯源处置建议 + 仪表盘                          |
| 4. 动态更新与安全（特征动态更新；加密存储；完整性校验）       | features/profiles 热更新 API + Fernet 加密 + HMAC 校验接口                  |
| （扩展）情报平台接入与扩线                       | intel 客户端 + enricher + expander + 报告 "情报支撑" 区块 + 图谱 "疑似关联资产" 节点    |



***

## 12. 风险与对策



| 风险                | 影响    | 对策                                  |
| ----------------- | ----- | ----------------------------------- |
| LLM API 不可用 / 超限  | 流程中断  | 规则模板兜底模式 + 重试退避                     |
| LLM 幻觉（编造组织 / 证据） | 结论不可信 | 证据引用强制 + RAG-lite 约束 + 置信度门限        |
| 模拟数据被质疑 "不像真的"    | 说服力不足 | 场景手法对齐公开 APT 报告；标注模拟口径；保留 labels 评测 |
| 事件窗口超出 token 上限   | 识别失败  | 窗口切分 + 摘要聚合                         |
| 前端开发耗时超预期         | 进度风险  | 先做 G6 图谱 + 报告页两个核心页，其余页面后补          |



***

## 13. 交付物清单



1. `backend/`：FastAPI 后端（含数据生成、服务、API、安全、SQLite）。

2. `frontend/`：Vue3 前端（仪表盘 / 时间线 / 报告 / 知识图谱 / 数据管理 / 大模型分析 / 密钥配置）。

3. `data/`：模拟数据集、特征规则库、组织画像库、评测脚本与结果、上传文件（uploads）。

4. `docs/`：本设计方案、README（运行说明）、答辩报告（后续由本方案扩展生成）。

5. 演示路径：`python -m uvicorn main:app` 启动后端 → `npm run dev` 启动前端 → 全流程演示。



***

## 14. 真实数据来源



1. **实际采用：OTRF Security-Datasets（主数据源）**

* OTRF（Open Threat Research）Security-Datasets：真实 Sysmon/Security 事件 NDJSON，按 APT 场景分目录（c2 / lateral_movement / persistence / trace_cleaning 等）。本项目已接入 7 个场景 44,024 条事件，经 EventID→type 映射为统一 Event 后参与全流程研判，并支持按数据源切换。

2. **候选 / 备选公开数据集**

* DARPA Transparent Computing（TC3 / TC5-8）：真实主机审计日志 + APT 场景标准答案；体积大，需切片导入。

* CICIDS2017 / CICIDS2019（UNB）：网络流量特征与 PCAP，含攻击场景，易用。

* MITRE ATT\&CK Evaluations（APT3、APT29 等仿真）：攻击仿真数据与结果报告。

* UNSW-NB15 等流量特征集（备选）。

1. **威胁情报 / IOC 类（构建画像库与增强证据）**

* 奇安信威胁情报中心：公开 APT 研究报告与公开 IOC。

* 微步在线：公开威胁报告（API 需额度）。

* abuse.ch：ThreatFox（IOC）、URLhaus（恶意 URL）、MalwareBazaar（样本）。

* AlienVault OTX：公开 pulse 与 API。

* VirusTotal 免费 API：信誉、DNS↔IP、检测结果。

* MITRE ATT\&CK（Groups/Software TTP 画像）；CISA KEV（已知被利用漏洞清单）。

3. **自建真实数据（推荐加分路径）**

* 本地虚拟机用 Atomic Red Team / MITRE CALDERA 复现 APT 场景，采集真实 Sysmon/Suricata 日志：真实行为数据且可控、有标准答案。

4. **数据组合建议**

* 公开数据集切片（行为主源）+ 公开报告 / OTX/abuse.ch（画像库与 IOC）+ 平台 API 实时查询（证据增强）+ 可选本地仿真复现（答辩亮点）。

* 所有外部数据注明来源与采集时间，避免版权与合规问题。



***

## 15. 情报平台接入与扩线设计（扩展功能）



1. **四平台接入方式与用途**



| 平台   | 接入方式     | 用途                            | 额度 / 注意    |
| ---- | -------- | ----------------------------- | ---------- |
| VT   | 免费 API   | IOC 信誉、DNS↔IP 互查、检测数          | 500 次 / 天  |
| 微步   | 威胁情报 API | IP / 域名恶意判定、威胁类型、家族           | 额度有限，需缓存限流 |
| FOFA | 高级 API   | 资产扩线（同证书 / 同 IP / 同指纹 / 关联域名） | 需账号额度      |
| 奇安信  | 无公开 API  | 公开报告 / 公开 IOC 导入画像库           | 不爬取网页      |



1. **功能流程**

* 增强：报告 IOC → 并行查询 VT / 微步 → 生成 "情报支撑" 表（平台 / 判定 / 得分 / 来源）→ 注入报告与 LLM 溯源证据。

* 扩线：确认恶意域名 / IP → FOFA 关联查询 → 候选资产 → 图谱新增 "疑似关联资产" 节点 → 人工复核。

* 画像库：公开 APT 报告 → LLM 辅助抽取 TTP / 工具 / 目标 → 写入 `apt_profiles.json`。

1. **前端扩展**

* 报告页新增 "情报支撑" 区块（多平台判定汇总表 + 来源链接）。

* 知识图谱新增 "疑似关联资产" 节点类型（虚线标识）+ 扩线关系边。

* 可选新增 "情报查询" 页：输入 IP / 域名，聚合展示多平台结果。

1. **合规与工程约束**

* API Key 存 `.env`（gitignore），不进库不入报告。

* 全部查询异步 + 限速 + SQLite 缓存；失败降级不影响主流程。

* 展示结果注明 "平台判定仅供参考"，保留人工复核环节。

* 本模块全新实现，不复用 AutoSearch/AutoReport/AutoSC 代码。